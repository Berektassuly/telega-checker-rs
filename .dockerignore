# Build artifacts
target/

# Git
.git/
.gitignore

# Environment (secrets)
.env

# Local SQLite databases
*.db
*.db-shm
*.db-wal

# Docker files (no need in build context)
docker-compose.yml
docker-compose.yaml
DEPLOY.md

# Nginx config (mounted separately, not needed in Rust build)
nginx/

# IDE / editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS junk
Thumbs.db
.DS_Store
